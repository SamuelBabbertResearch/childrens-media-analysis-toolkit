# CMAT — Repository map

Where things are, and what to edit for a given kind of change. Update this file
whenever files move, a folder changes purpose, or a system is added.

---

## Top-level folders

| Folder | Purpose |
|---|---|
| `analyzer/` | The engine and data model. **Imports no GUI framework** — enforced by `tests/test_engine_isolation.py` |
| `ui/` | The Qt (PySide6) front-end — every screen now exists here; not yet the interface that has been *used* |
| `tests/` | pytest suite |
| `validation/` | Hand coding, codebooks, and trial manifests — **authoritative data a person typed** |
| `design/` | Specifications and strategy notes — the pipeline north-star spec, the migration strategy, the UX audit, positioning. **Inputs, not authority**; see `design/README.md` |
| `docs/` | Long-form documentation; `docs/wiki`. **Gitignored** — managed separately, so anything moved here leaves version control |
| `Shows/` | The episode library for this working copy (`<root>` in the docs) |
| `pipelines/` | Fallback pipeline documents — **gitignored, personal project data** |
| `_site/` | The **Open Children's Media Index** — the published dataset site, generated by `build_site.py` |
| `build/`, `dist/` | PyInstaller output — never edited by hand |

## What to edit, by kind of change

| I want to… | Edit |
|---|---|
| change how a metric is computed | the relevant `analyzer/metrics_*.py`, then `analyzer/measurements.py` |
| add a metric | `analyzer/schema.py`, its `metrics_*` module, `analyzer/measurements.py`, `ui/report.py`, `ui/settings.py` labels |
| change what a screen looks like | `ui/theme.py` (tokens in `ui/tokens.py`) — read `ui/DESIGN.md` §0 first |
| add or change a pipeline stage type | `NODE_TYPES` in `analyzer/pipeline_graph.py` — a dict entry, not a UI change |
| change how pipeline status is derived | `analyzer/pipeline.py` |
| change which episodes a screen works on | `analyzer/scope.py` for what a scope is; `MainWindow.set_scope` for which one is current |
| change how episodes are grouped into eras | `analyzer/eras.py`; the editor is `ui/eras.py` |
| change scoring (weights, ceilings) | `config.json` presets; the dialog is `ui/settings.py` |
| change which tool measures what | `analyzer/measurements.py` registry; the dialog is `ui/measurements.py` |
| change the analysis report | `ui/report.py` — no Qt import, testable headless |
| add a command-line entry point | `cli.py` — never duplicate engine logic in a front-end |

## Engine — `analyzer/`

| Module | Role |
|---|---|
| `engine.py` | per-episode analysis; dispatches to the selected tool per measurement |
| `batch.py` | whole-show runs with a progress callback |
| `measurements.py` | registry: which tool produces each measurement, with what parameters, and whether it is validated. Fingerprints settings so stale cache is detectable |
| `metrics_cuts.py` | shot boundaries, dissolves, cut classification |
| `metrics_frames.py` | colour, motion, flashing (shared frame pass) |
| `metrics_audio.py` | RMS loudness, dynamic range (via FFmpeg) |
| `speech.py`, `vocab_complexity.py` | WPM and speech density; readability, tiers, AoA, MTLD |
| `metrics_sensory.py` | the weighted composite; `effective_weights()` is what anything DISPLAYING a breakdown must use |
| `aggregate.py` | show-level statistics (`ShowAggregate`) |
| `cache.py`, `db.py` | disk cache and SQLite index; staleness detection. **`load_scored()` is the only sanctioned way to read a cached result** — it re-derives the composite |
| `show_index.py` | folder discovery (one level of category nesting) |
| `schema.py` | `EpisodeResult`, `ShowAggregate`, `MetricStats` |
| `sampler.py`, `trials.py` | reproducible episode sampling; the run registry |
| `eras.py` | named date ranges -> `Episode.extra['era']`, so a long run can be stratified by production period |
| `validation.py`, `event_coding.py` | tool-vs-human scoring, Cohen's kappa, inter-coder agreement |
| `pipeline.py` | **derived** workflow status from what is on disk |
| `pipeline_graph.py` | **editable** pipeline documents, node types, templates |
| `scope.py` | the **research context** — which episodes the application is working on. The only reader of a draw's `selected.csv`; normalises paths at the choke point so a sample's spelling matches the library walk's |
| `provenance.py` | self-reported accuracy figures — read by the PDF, the CSV sidecar, the JSON export and the public site. Keep `METRIC_STATUS` in step with the registry; a test enforces it |
| `prefs.py` | per-user local state, kept out of the versioned config |
| `config_loader.py` | loads and normalises `config.json` |
| `optional_tools.py`, `detector_transnet.py` | optional dependency registry; TransNetV2 wrapper |
| `report_pdf.py` | PDF export |
| `wiki_importer.py`, `tvmaze_importer.py` | episode metadata import |
| `intro_templates.py` | reusable coding-sheet templates |
| `ffmpeg_path.py` | locates the FFmpeg binary |

## Interface — `ui/`

| File | Role |
|---|---|
| **`DESIGN.md`** | **the visual specification — §0 is the recipe for building a screen; read before porting anything** |
| `reference/*.css` | the supplied design mockups' stylesheets, extracted verbatim. **The source of the design, not a copy of it** |
| `reference_css.py` | loads those, resolves `var()`, returns a component's rules |
| `tokens.py` | design tokens; **no framework imports** — shared by both front-ends |
| `theme.py` | fonts, the Qt stylesheet |
| `main_window.py` | the shell: title bar, toolbar, tabs, Library, Full Series Aggregate, and the pipeline tab's actions. `STAGE_TABS` / `STAGE_ACTIONS` map a stage to the screen that does its work; `SubViews` builds the screens-inside-a-tab switcher |
| `modal.py` | `WindowTitleBar`, the dialog frame, `ConfirmDialog` |
| `native_frame.py` | suppresses the Win32 frame's *drawing* while keeping snap, resize and the system menu |
| `pipeline_view.py` | the node canvas: nodes, wires, pan, zoom |
| `inspector.py` | the pipeline inspector panel — the selected stage's derived status, details and next action |
| `report.py` | episode, show and **comparison** results as HTML; **no Qt import**, so it is testable headless and reusable for PDF export and the static site |
| `automated.py` | the Automated coding tab, the analysis queue, Transcribe Missing Subtitles, and the worker threads |
| `index_tab.py` | the Index tab over the SQLite index; `focus_episode()` selects one row for the Library's *Show in Index* |
| `handcoding.py`, `player.py` | the Human coding tab — Code, Validate tool, Agreement; VLC embedded in Qt |
| `language.py` | the Language tab — Speech (from cache) and Vocabulary (spaCy on a worker thread) |
| `sampler.py` | the Episode Sampler dialog; explanations come from `analyzer.sampler.TOOLTIPS` |
| `trials_tab.py` | the Trials registry |
| `settings.py` | Presets & Weights — **scoring settings only** |
| `measurements.py` | **Measurement settings** — which tool, which parameters. The other axis: these make cached results stale |
| `optional_tools.py` | optional-dependency registry with an install log |
| `metadata_import.py` | Wikipedia / TVMaze episode metadata import; fuzzy matches are flagged and unchecked-by-choice |
| `compare.py` | two episodes or two shows side by side; rendering is `report.compare_html` |
| `eras.py` | define a show's production eras; counts each era's episodes as you type |
| `welcome.py` | the starting-layout wizard |
| `chart.py` | matplotlib dialogs: per-episode components, speech rate, vocabulary |

## Entry points

```
python -m pytest -q                    the test suite — run before and after any change


python gui.py                          the Tk build — THE CURRENT SOFTWARE
python cmat_qt.py                      the Qt build — full coverage, not yet the product

python cli.py analyze <file.mp4>       single episode → JSON
python cli.py analyze <show_folder>    batch → per-episode JSON + aggregate
python cli.py db episodes <root>       list indexed episodes
python cli.py db shows <root>          list indexed shows

python validate_cuts.py …              template / export / compare / sweep / summary
python code_events.py …                template / rates / agreement / publish
python build_site.py                   static site
```

What each script is for:

| Script | Role |
|---|---|
| `cmat_qt.py` / `gui.py` | application entry points (Qt / Tk) |
| `cli.py` | headless analysis and index queries |
| `build_site.py` | builds the **Open Children's Media Index** from the corpus |
| `validate_cuts.py` | transition validation against hand coding |
| `code_events.py` | fantastical-event coding: rates, agreement, publish |
| `sample_youtube.py` | YouTube sampling list by upload-date spread — **list only, no downloads** |
| `patch_speech_cache.py` | back-fills speech metrics from caption files. Takes a library root, defaults to the remembered one, supports `--dry-run` |

## Legacy Tk front-end

`gui.py` plus 14 `gui_*.py` modules. Complete, and **no longer the only home
of any screen** — as of 2026-08-14 every one has a `ui/` equivalent. They stay
on disk because coverage is not mileage: the Qt Language, Validate tool,
Agreement and Sampler screens have each been driven once, headless. Delete a
module only after its Qt screen has done real work (`TODO.md` items 9 and 11).

Both builds read the same project, so there is a real risk of editing the
wrong one — check which file you are in before changing a screen.

## Documents

| File | What it is |
|---|---|
| `CLAUDE.md` | the rulebook — rules only |
| `INDEX.md` | retrieval table of contents — start here |
| `onboarding.md` | previously-on, for the next session |
| `TODO.md` | what is next, now |
| `ROADMAP.md` | longer-term plan and positioning |
| `ARCHITECTURE.md` | how the system is put together |
| `STACK.md` | technology reference |
| `DECISIONS.md` | decision log |
| `LEARNINGS.md` | mistake and lesson log |
| `navigation.md` | this file |
| `README.md` | public-facing description |
| `validation/CODEBOOK.md`, `validation/EVENT_CODEBOOK.md` | hand-coding definitions |
| `FOR_PAPER.txt` | paper notes — **never committed** |
